go_import_path: github.com/kevinburke/nacl

language: go

os: linux
dist: trusty

sudo: required

before_script:
     - make install-travis

script:
  - make ci
  - make clean-cache

cache:
  directories:
    - $HOME/.bzrepos
    - $HOME/.cache/bazel
